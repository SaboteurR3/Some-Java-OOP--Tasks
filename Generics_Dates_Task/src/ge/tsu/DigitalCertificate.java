package ge.tsu;

import java.time.LocalDate;

public class DigitalCertificate extends Certificate {
    public DigitalCertificate(String text, LocalDate issueDate, LocalDate expireDate) {
        super(text, issueDate, expireDate);
    }
}
// 